- ensure you have uv install and run this command to install dependencies. I already have gradio and openi installed,
 > uv sync 

 - to launch app, run the command. I already did a sample flow, it will give you the gradio UI url
 > uv run python main.py

- Add your environment variable name to the example.env (see sample with OPENAI that I added) so everyone can add it to their local .env 

